export interface Product {
  id: string;
  title: string;
  image: string;
  text: string;
}

export interface Post {
    id: string,
    rate: number,
    text: string,
    id_user: string,
    id_entry: string
}

